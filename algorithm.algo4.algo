FUNCTION PALINDROME (STR: STRING) : BOOLEAN
 var
     1: INTEGER;
BEGIN
  IF ( STR[i]= 1)THEN
     RETURN TRUE;
 END_IF
 IF (STR[i]=STR[i-1]) THEN
     RETURN PALINDROME (STR[i ,i-1])
 ELSE
    RETURN FALSE;
 END_IF
END
